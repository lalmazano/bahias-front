import 'package:flutter/material.dart';
class ReservasPage extends StatelessWidget {
  const ReservasPage({super.key});
  @override
  Widget build(BuildContext context) =>
      const Center(child: Text('Reservas', style: TextStyle(fontSize: 22)));
}
